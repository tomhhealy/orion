# Convex

Temporary `_generated` files in this directory are placeholders so the app can compile before a real Convex deployment is linked.

Replace them by running:

```bash
bun run dev:convex
```

Once `convex dev` is connected to a project, Convex will regenerate `convex/_generated/*` with the real API surface.
